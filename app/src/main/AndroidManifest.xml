<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.cybershift.ibest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Campaign.MakeCampaignActivity"></activity>
        <activity android:name=".VolunteerData.ThankYouActivity" />
        <activity android:name=".VolunteerData.VolunteerData3Activity" />
        <activity android:name=".VolunteerData.VolunteerData2Activity" />
        <activity android:name=".VolunteerData.VolunteerData1Activity" />
        <activity android:name=".ChooseRoleActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>